CREATE PROCEDURE [dbo].[set_statements]
as
	
		set transaction isolation level read uncommitted;
SET QUOTED_IDENTIFIER ON;


SET NOCOUNT OFF;

set xact_abort on;

set ansi_padding off set numeric_roundabort on

set quoted_identifier off;